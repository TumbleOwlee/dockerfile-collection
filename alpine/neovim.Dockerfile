FROM alpine:latest
RUN apk add --no-cache build-base cmake coreutils curl unzip gettext-tiny-dev git wget && git clone https://github.com/neovim/neovim && cd neovim && git checkout stable && make CMAKE_BUILD_TYPE=Release && make install